**中文** · [English](TODO.en.md)

# 待完成的测量

按**"结果能不能改变某个决定"**排序，不是按工作量。每项写明：为什么重要、怎么跑、结果填回哪里、以及**什么结果会导致什么行动**。

前三项能改变现有结论或配置；后三项是补强证据，做不做取决于你在意到什么程度。

---

## 0. ~~先验证工具本身~~ ✅ 已完成（2026-09-02）

三个脚本都在真实服务上跑通了。原先列的六个未验证点，五个通过、一个有缺陷：

| 点 | 结果 |
|---|---|
| `chat_ttft()` 流式 SSE 解析、首 chunk 判定 | ✅ |
| `engine_metrics()` 解析 `Avg prompt throughput` | ✅ |
| `max_tokens=64` 触发首 chunk | ✅ |
| 两个脚本的 `--md` 格式 | ✅ 可直接粘 |
| `loadtest.py` 并发路径与守卫 | ✅ |
| **`long_prompt()` 的 token 估算** | ❌ **偏低约 24%** |

### ✅ 已修（2026-09-02）

**`long_prompt()` 系数**：改用 5.26 字符/token，三档实测命中 100–101%。
（原先记的"改 3 字符"是**反了** —— 系数 4 得 76%、系数 3 只得 57%，
应当往大改。这条自我纠正本身值得记：反推方向搞错时，两次实测立刻暴露。）

**输出正确性比对**：`_common` 新增 `chat_text()`（返回文本而非计时指标），
`bench-baseline.py` 新增 `verify` 子命令：

```bash
./bench-baseline.py verify --save   # 建基准
./bench-baseline.py verify          # 比对，不一致时退出码 1
```

**顺带标定了本部署的可复现性**：四条比对题各连跑 8 次，每条只产生 1 种
输出 —— **temperature=0 下逐字比对这个方法成立**。但选题极易踩坑，三类
被淘汰的题目记在脚本注释里（多解、指令歧义、近似平局）。

**其中一条修正了既有结论**：2026-09-02 评估 B12X 稀疏索引器时报的"输出
不一致"是**假阳性** —— 那条长代码生成题本身就不确定，同一配置连跑两次
也会在第 788 字符出现同样差异。索引器没有算错，回退理由只有性能。

### ~~待修：`long_prompt()` 的估算系数~~

`4 字符 ≈ 1 token` 估得太宽，四档实测一致落在目标的 **76%**：

| 目标 | 实际 prompt_tokens |
|---|---|
| 1,000 | 768 |
| 8,000 | 6,101 |
| 32,000 | 24,361 |
| 100,000 | 76,068 |

改成 **3 字符/token** 即可。不影响已有数据（脚本报告的是实际 `prompt_tokens`），但档位名称目前要理解成"约 76%"。

### 新发现：没有任何工具能验证输出正确性

`_common.chat()` **只返回计时指标**（`wall` / `out` / `inp` / `tps`），不返回文本内容。所以仓库里现有的四个工具**都无法做输出比对** —— 而"能跑但算错"恰恰是笔记反复强调的头号风险（8.6 节 `COMPRESSED_MLA` 先例）。

2026-09-02 验证 B12X 稀疏索引器时只能临时直接调 API 绕开。**建议给 `_common` 加一个返回 content 的函数**，并在 `bench-baseline.py` 里加一个 `--verify` 子命令：固定提示词、`temperature=0`、与基准逐字比对。

---

## 1. 预填充与 TTFT

**现状**：笔记[第 5.6 节](docs/deployment-notes.md#56-预填充与-ttft)引用的 8K→1,513 / 32K→2,284 / 100K→2,639 tok/s **是上游数据，本机从未测过**。这是笔记里唯一还依赖外部数字的性能章节。

**为什么重要**：TTFT 是长上下文 agent 场景里用户直接感受到的量。当前对"60K 提示词多久出第一个字"没有任何本机依据。

```bash
PROFILE=dsv4f ./scripts/measure-prefill.py ttft --md
```

扫 1K / 8K / 32K / 100K，每档 3 次取中位数，同时读引擎自己报的 `Avg prompt throughput`。

**结果填回**：第 5.6 节，替换掉现在的上游数据，并记上配置四元组与日期。

**什么结果会导致什么行动**：

- 若"随深度变快"在本机也成立 → 确认上游结论，长上下文不必特殊对待
- 若不成立（TTFT 随长度超线性增长）→ 长上下文场景需要重新评估，可能要限制 `MAX_MODEL_LEN` 的实际使用范围

---

## 2. ~~长预填充对短请求的阻塞~~ ✅ 已测（2026-09-02）

**结论：推翻了第 8.4 节原先的判断，`--long-prefill-token-threshold 1024` 已进入生产配置。**

不设阈值时短请求被堵 **228.6×**（76K 在途 → 等 38.1s，而长请求自己也才 38.9s，重叠 98%）。分块预填充没有起到让路作用，日志里短请求一直卡在 `Waiting` 队列。

设 1024 之后：

| | 收益 | 代价 |
|---|---|---|
| 短请求阻塞 | 38.1s → **1.69s（−96%）** | — |
| 阻塞随长度增长 | 229× → **常数 9×** | — |
| 长请求 TTFT | — | 38.88s → **49.66s（+28%）** |
| 单流基线 | 四项全在噪声内 | 无 |

**顺带查清了它为什么长期没被评估**：自动赋值挂在 `max_num_partial_prefills > 1` 下面，而那个开关在 V1 上是死的，所以不显式传参就永远是 0。完整分析见笔记 8.4 节。

---

## 3. ~~`VLLM_USE_B12X_SPARSE_INDEXER=1`~~ ✅ 已测（2026-09-02）

**结论：不要开。三项指标全部变差，零收益。**

| 指标 | 关（生产） | 开 | 变化 |
|---|---|---|---|
| 100K TTFT 中位 | 38.88s | 41.14s | **+5.8% 更慢** |
| 32K TTFT 中位 | 11.98s | 12.32s | +2.8% 更慢 |
| KV 池 | 1,181,189 token | 1,024,435 | **−13%** |
| count 单流 | 86.7 tok/s | 84.3 | −2.8% |
| code 单流 | 71.6 | 71.8 | +0.3%（噪声内）|

开它的唯一动机是加速长上下文预填充，**而它让预填充变慢了**。同时还要付 13% 的 KV 池。

**正确性没有问题。** 四组固定提示词 `temperature=0` 逐字比对，三组完全一致；第四组差异出现在第 788 字符，是 docstring 示例间少了一个空行 —— 换内核带来的数值微扰翻转了一个 token，两份代码都合法。**这不是 8.6 节 `COMPRESSED_MLA` 那类"能跑但算错"。**

**启动自检报过接受长度 3.29（低于 3.5 门槛），但那是单次测量，不可信** —— 随后的单流基线四项几乎没动（若接受长度真的从 6.00 掉到 3.29，按吞吐模型 count 应下跌约 45%，实测只有 −2.8%）。这条正好复现了方法论第 1 条：单次测量不是结论。

**测量方法值得复用**：验收用的是 **100K 档 TTFT**，三次离散度仅 0.4%，远优于并发聚合指标的 ±11%。以前这一项"测不动"就是因为没有足够灵敏的指标。

---

## 4. `FUSED_MARKOV_ARGMAX` 的严格 A/B

**现状**：当前结论是"中性"，但**证据不对称** —— `FUSED=0` 那侧只有 1 个样本，`FUSED=1` 侧有 3 个。笔记里已经诚实标注了保留理由是"理论省显存带宽 + 实测无害"，**不是"实测有收益"**。

**为什么可能不值得做**：上游报的收益是 +1.8% 单流，而本机单流基线的噪声地板是 ±1% —— 效应量只有噪声的 2 倍，要分辨需要两侧各十几轮。

```bash
# 每侧至少 5 轮，中间重启切换开关
PROFILE=dsv4f ./scripts/bench-baseline.py warmup
PROFILE=dsv4f ./scripts/bench-baseline.py bench --md
```

**什么结果会导致什么行动**：

- 确认中性 → 可以考虑改回默认 `0`，减少一个未经证实的偏离
- 确认有收益 → 把第 8.1 节的"中性"改成实测数字

---

## 5. KV 池的 page cache 假说

**现状**：笔记[第 5.7 节](docs/deployment-notes.md#57-kv-池期望值与噪声)记录了同配置启动间 **11% 的波动**，但**成因从未隔离**。曾怀疑是 page cache 残留，但那次重启同时改了 cudagraph 记账，两个因素把池子往相反方向推，无法归因。

**怎么测**：需要一组**只有缓存状态不同**的对照 —— 配置完全不变，一次在 `drop_caches` 之后启动，一次在不清缓存的情况下启动，各重复几次。

**代价**：每次启动约 6 分钟，要拿到可判读的结果至少 6 次 → 约 40 分钟机时。

**什么结果会导致什么行动**：

- 若确认是 page cache → 启动纪律里的 `drop_caches` 步骤有了实测依据（目前是照抄上游配方）
- 若不是 → 11% 波动另有成因，值得继续查，因为它决定了所有 KV 相关结论需要多少样本

---

## 6. gmu 下一档 0.8010

**现状**：笔记[第 8.2 节](docs/deployment-notes.md#82-gmu-与-cudagraph-记账必须一起改)记录，当前 0.7935 下引擎建议的下一档是 0.8010（记账开），换算成关闭口径约 0.7935，仍低于竞赛实测的 0.80 物理边缘。

**未做的理由**：收益递减，而 KV 池的测量噪声是 11% —— 预期增益大概率淹没在噪声里。

**若要做**：必须多次启动取中位数，否则测不出来（这正是第 5.7 节的教训）。

---

# 已知强度不足的结论

**不是待办，是提醒** —— 这些结论目前的证据到什么程度：

| 结论 | 证据强度 | 缺什么 |
|---|---|---|
| Patch A 单流 +6% | **确凿** | — 三次离散度 1%，远超噪声 |
| Patch A 并发侧偏正 | **倾向** | 并发指标 ±11% 且偶发 2 倍异常，3 轮只够说方向 |
| `FUSED_MARKOV=1` 中性 | **弱** | off 侧只有 1 个样本（见上面第 4 项） |
| 预填充调度两参数证伪 | **机制确凿，代价未量** | `max_num_partial_prefills` 在 V1 上零引用是硬事实；但"短请求被堵多久"没测（第 2 项） |
| cudagraph 收窄中性 | **确凿** | — 收益与代价都在噪声之下，且回退实验验证过 |
| 预填充随深度变快 | **本机无数据** | 引用的是上游数字（第 1 项） |

---

# 测量纪律提醒

跑上面任何一项之前，先读笔记[第 6 节](docs/deployment-notes.md#6-测量纪律)。三条最容易忘的：

1. **任何性能结论至少 3 次取中位数** —— 本机会产生 2 倍的瞬态异常值
2. **报数字必须同时记录配置四元组、接受长度、温度**
3. **先知道你选的指标的噪声地板** —— 效应量小于地板时，实验无论跑多少次都只能产出"无差异"
