**中文** · [English](TODO.en.md)

# 待完成的测量

按**"结果能不能改变某个决定"**排序，不是按工作量。每项写明：为什么重要、怎么跑、结果填回哪里、以及**什么结果会导致什么行动**。

前三项能改变现有结论或配置；后三项是补强证据，做不做取决于你在意到什么程度。

---

## 0. 先验证工具本身 ⬅ 其余各项都依赖它

> ### ⚠️ 四个 Python 工具都没有在真实服务上完整跑通过一次
>
> 它们共用 `_common.py` 里的请求与解析路径，而这套代码只经过语法检查、配置加载和并发守卫的测试——**没有一个脚本完整跑完过一次真实请求**；`measure-prefill.py` 更是从未执行过。
>
> **能跑到网络调用不等于能跑完。** 这正是这套笔记反复强调的那类错误——验证方式没有覆盖改动的类型（笔记[第 10 节第 25 条](docs/deployment-notes.md#10-方法论)）。

**第一次跑真实服务时，按这个顺序确认：**

```bash
PROFILE=dsv4f ./scripts/bench-baseline.py bench --md    # 最简单，先验证请求路径
PROFILE=dsv4f ./scripts/loadtest.py uniform 2           # 再验证并发路径
PROFILE=dsv4f ./scripts/measure-prefill.py ttft --md    # 最后验证新脚本
```

**具体没验证过的东西：**

| 位置 | 未验证的点 | 出问题会怎样 |
|---|---|---|
| `_common.chat()` | 返回的字段名与实际响应是否对得上 | KeyError |
| `_common.chat_ttft()` | 流式 SSE 解析、首个 chunk 的判定 | TTFT 恒为 `None`，表格全是"—" |
| `_common.engine_metrics()` | 能否正确解析 `Avg prompt throughput` 那行 | 引擎预填充列全是"—" |
| `_common.long_prompt()` | 生成的提示词实际 token 数是否接近目标 | 长度档位名不副实，`4 字符 ≈ 1 token` 只是估算 |
| `measure-prefill.py ttft` | `max_tokens=64` 是否足够稳定触发首个 chunk | 部分轮次拿不到 TTFT |
| 两个脚本的 `--md` | 实际输出的 markdown 格式 | 粘进笔记后表格错位 |

**发现问题就地修，并把修法记进 CHANGELOG** —— 这些是工具缺陷，不是测量结果。

---

## 1. 预填充与 TTFT

**现状**：笔记[第 5.6 节](docs/deployment-notes.md#56-预填充--本机未做专门测量)引用的 8K→1,513 / 32K→2,284 / 100K→2,639 tok/s **是上游数据，本机从未测过**。这是笔记里唯一还依赖外部数字的性能章节。

**为什么重要**：TTFT 是长上下文 agent 场景里用户直接感受到的量。当前对"60K 提示词多久出第一个字"没有任何本机依据。

```bash
PROFILE=dsv4f ./scripts/measure-prefill.py ttft --md
```

扫 1K / 8K / 32K / 100K，每档 3 次取中位数，同时读引擎自己报的 `Avg prompt throughput`。

**结果填回**：第 5.6 节，替换掉现在的上游数据，并记上配置四元组与日期。

**什么结果会导致什么行动**：

- 若"随深度变快"在本机也成立 → 确认上游结论，长上下文不必特殊对待
- 若不成立（TTFT 随长度超线性增长）→ 长上下文场景需要重新评估，可能要限制 `MAX_MODEL_LEN` 的实际使用范围

---

## 2. 长预填充对短请求的阻塞

**现状**：笔记[第 8.4 节](docs/deployment-notes.md#84-已证伪预填充调度两参数)从源码推出了 `long_prefill_token_threshold` 的机制（每步 token 硬上限，6 万 token 的预填充从约 8 步变约 59 步），据此判定它是"延迟换吞吐的权衡"。

**但权衡的另一半从未量过** —— 不开这个参数时，短请求实际被长预填充堵多久？没有这个数，"值不值"就只是推论。

```bash
PROFILE=dsv4f ./scripts/measure-prefill.py blocking --md
```

先测空闲时短请求 TTFT 作基准，再在 8K / 32K / 100K 的长请求预填充在途时重测，看比值。

**结果填回**：第 5.6 节，并回头更新第 8.4 节的结论强度。

**什么结果会导致什么行动**：

- **比值接近 1** → chunked prefill 已经让短请求插进去了，`--long-prefill-token-threshold` **确定不需要**，第 8.4 节的"证伪"可以从"机制推论"升级为"实测确认"
- **比值很大**（比如 5× 以上）→ 长预填充在独占调度步，这个参数**值得重新考虑**，需要单独一次重启 + 混合负载 A/B

---

## 3. `VLLM_USE_B12X_SPARSE_INDEXER=1`

**现状**：笔记[第 8.7 节](docs/deployment-notes.md#87-未测的候选)。这是 B12X 系列里**唯一既没有警告记录、上游竞赛也没测过**的开关。所有门槛都已确认通过：`is_device_capability_family(120)` 对 SM121 返回 True，`use_fp4_indexer_cache` 默认 False。

**为什么值得试**：同系列的 `VLLM_USE_B12X_MOE` 收益是 55+ vs 29 tok/s（近 2 倍）。

> ### ⚠️ 验收必须比对输出正确性，不能只看速度
>
> B12X 系列有 **"能跑但算错"** 的先例：`VLLM_DSV4_B12X_COMPRESSED_MLA=1` 在上游竞赛 E12 里输出错误 + CUDA assert。
>
> 做法：同一组提示词、`temperature=0`，开关前后各跑一遍，**逐字比对 `content`**。速度提升但输出变了 = 立即回退。

**结果填回**：第 8.1 节总表 + 第 8.6/8.7 节。

---

## 4. `FUSED_MARKOV_ARGMAX` 的严格 A/B

**现状**：当前结论是"中性"，但**证据不对称** —— `FUSED=0` 那侧只有 1 个样本，`FUSED=1` 侧有 3 个。笔记里已经诚实标注了保留理由是"理论省显存带宽 + 实测无害"，**不是"实测有收益"**。

**为什么可能不值得做**：上游报的收益是 +1.8% 单流，而本机单流基线的噪声地板是 ±1% —— 效应量只有噪声的 2 倍，要分辨需要两侧各十几轮。

```bash
# 每侧至少 5 轮，中间重启切换开关
PROFILE=dsv4f ./scripts/bench-baseline.py warmup
PROFILE=dsv4f ./scripts/bench-baseline.py bench --md
```

**什么结果会导致什么行动**：

- 确认中性 → 可以考虑改回默认 `0`，减少一个未经证实的偏离
- 确认有收益 → 把第 8.1 节的"中性"改成实测数字

---

## 5. KV 池的 page cache 假说

**现状**：笔记[第 5.7 节](docs/deployment-notes.md#57-kv-池期望值与噪声)记录了同配置启动间 **11% 的波动**，但**成因从未隔离**。曾怀疑是 page cache 残留，但那次重启同时改了 cudagraph 记账，两个因素把池子往相反方向推，无法归因。

**怎么测**：需要一组**只有缓存状态不同**的对照 —— 配置完全不变，一次在 `drop_caches` 之后启动，一次在不清缓存的情况下启动，各重复几次。

**代价**：每次启动约 6 分钟，要拿到可判读的结果至少 6 次 → 约 40 分钟机时。

**什么结果会导致什么行动**：

- 若确认是 page cache → 启动纪律里的 `drop_caches` 步骤有了实测依据（目前是照抄上游配方）
- 若不是 → 11% 波动另有成因，值得继续查，因为它决定了所有 KV 相关结论需要多少样本

---

## 6. gmu 下一档 0.8010

**现状**：笔记[第 8.2 节](docs/deployment-notes.md#82-gmu-与-cudagraph-记账必须一起改)记录，当前 0.7935 下引擎建议的下一档是 0.8010（记账开），换算成关闭口径约 0.7935，仍低于竞赛实测的 0.80 物理边缘。

**未做的理由**：收益递减，而 KV 池的测量噪声是 11% —— 预期增益大概率淹没在噪声里。

**若要做**：必须多次启动取中位数，否则测不出来（这正是第 5.7 节的教训）。

---

# 已知强度不足的结论

**不是待办，是提醒** —— 这些结论目前的证据到什么程度：

| 结论 | 证据强度 | 缺什么 |
|---|---|---|
| Patch A 单流 +6% | **确凿** | — 三次离散度 1%，远超噪声 |
| Patch A 并发侧偏正 | **倾向** | 并发指标 ±11% 且偶发 2 倍异常，3 轮只够说方向 |
| `FUSED_MARKOV=1` 中性 | **弱** | off 侧只有 1 个样本（见上面第 4 项） |
| 预填充调度两参数证伪 | **机制确凿，代价未量** | `max_num_partial_prefills` 在 V1 上零引用是硬事实；但"短请求被堵多久"没测（第 2 项） |
| cudagraph 收窄中性 | **确凿** | — 收益与代价都在噪声之下，且回退实验验证过 |
| 预填充随深度变快 | **本机无数据** | 引用的是上游数字（第 1 项） |

---

# 测量纪律提醒

跑上面任何一项之前，先读笔记[第 6 节](docs/deployment-notes.md#6-测量纪律)。三条最容易忘的：

1. **任何性能结论至少 3 次取中位数** —— 本机会产生 2 倍的瞬态异常值
2. **报数字必须同时记录配置四元组、接受长度、温度**
3. **先知道你选的指标的噪声地板** —— 效应量小于地板时，实验无论跑多少次都只能产出"无差异"
